name = "padelis-proxy"
main = "src/worker.ts"
compatibility_date = "2025-01-01"

[vars]
# Secrets — set via `wrangler secret put <NAME>`, never committed here:
#   PROXY_TOKEN
#   KAUNOPADELIS_LOGIN
#   KAUNOPADELIS_PASSWORD
